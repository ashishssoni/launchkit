# GitHub Launch Checklist

## Repo settings

- Repo name: `launchkit`
- Default branch: `dev`
- Description:
  - `Production-ready NestJS SaaS backend starter with auth, multi-tenancy, RBAC, Stripe billing, API keys, audit logs, and workspace notifications.`
- Topics:
  - `nestjs`
  - `nodejs`
  - `typescript`
  - `saas`
  - `backend`
  - `multi-tenant`
  - `rbac`
  - `stripe`
  - `prisma`
  - `postgresql`
  - `fastify`
  - `swagger`
  - `jwt`
  - `webhooks`

## Push branch

```bash
git remote add origin git@github-launchkit:ashishssoni/launchkit.git
git push -u origin dev
```

## After push

- Add LaunchKit as your first pinned repository
- Add a clean repo social preview image
- Add Swagger screenshot to README later
- Keep commits feature-based
- Open future work through PRs into `dev`

## Best portfolio angle

Use LaunchKit in proposals as proof for:

- SaaS backend architecture
- JWT auth and secure session handling
- Multi-tenant workspace design
- RBAC and protected business actions
- Stripe subscriptions and webhooks
- API key lifecycle management
- Auditable backend systems
